#include "main.h"

int _strlen(char *s) {
    /* Use s to avoid the unused parameter warning */
    (void)s; /* This explicitly tells the compiler we're not using s yet */
    return 0; /* Placeholder return value */
}

